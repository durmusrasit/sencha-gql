import { Theme } from "../types";

const themes: Theme[] = [
  { themeName: "dark", backgroundColor: "#000", foregroundColor: "#fff" },
  { themeName: "light", backgroundColor: "#fff", foregroundColor: "#000" },
];

export { themes };
